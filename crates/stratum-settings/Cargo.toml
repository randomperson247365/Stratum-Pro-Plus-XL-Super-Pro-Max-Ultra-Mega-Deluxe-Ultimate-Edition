[package]
name = "stratum-settings"
version = "0.1.0"
edition = "2021"

[dependencies]
stratum-config  = { path = "../stratum-config" }
iced            = { version = "0.13", features = ["tokio"] }
anyhow          = { workspace = true }
